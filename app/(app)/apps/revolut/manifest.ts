import { AppManifest } from "../common"

export const manifest: AppManifest = {
  name: "Revolut Business Sync",
  description: "Connect your Revolut Business account to automatically import bank transactions into TaxTicks.",
  icon: "Landmark",
}
